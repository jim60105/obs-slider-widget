## 1. CSS Glow Styles

- [x] 1.1 Add multi-layered `text-shadow` glow using `var(--theme-color)` to the `.progress-title` rule in the `<style>` block of `index.html`
- [x] 1.2 Add multi-layered `text-shadow` glow using `var(--theme-color)` to the `.progress-status` rule in the `<style>` block of `index.html`

## 2. Inline Style Glow

- [x] 2.1 Update the JavaScript that sets the `#displayPercent` inline style to include a `text-shadow` glow property using the current theme color value

## 3. Verification

- [x] 3.1 Verify all three text elements display a visible glow effect on page load
- [x] 3.2 Verify the glow color updates in real time when the theme color picker value changes
