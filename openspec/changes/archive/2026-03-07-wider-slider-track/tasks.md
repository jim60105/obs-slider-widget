## 1. Update Glass Track Width and Border

- [x] 1.1 Change `.glass-track` width from `20px` to `40px` in `index.html`
- [x] 1.2 Change `.glass-track` border from `2px solid rgba(255, 255, 255, 0.5)` to `4px solid rgba(255, 255, 255, 0.5)` in `index.html`

## 2. Verification

- [x] 2.1 Open `index.html` in a browser and verify the glass track renders at 40px width with 4px border
- [x] 2.2 Verify the capsule shape (border-radius 999px) still appears correct at 40px width
- [x] 2.3 Verify the fill animation still works correctly with the wider track
- [x] 2.4 Verify the title text and percentage display layout is not broken by the wider track
