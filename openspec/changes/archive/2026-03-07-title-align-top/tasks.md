## 1. Update Layout Class

- [x] 1.1 In `index.html` line 175, change `items-end` to `items-start` on the flex container wrapping the progress bar widget

## 2. Verification

- [x] 2.1 Open the widget in a browser and confirm the vertical title text is aligned to the top of the progress bar area
