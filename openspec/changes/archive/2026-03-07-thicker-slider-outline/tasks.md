## 1. Update Glass Track Border

- [x] 1.1 In `index.html`, change `.glass-track` border from `1px solid rgba(255, 255, 255, 0.5)` to `2px solid rgba(255, 255, 255, 0.5)`
- [x] 1.2 Update `.glass-track` box-shadow outer glow from `0 0 12px rgba(255, 255, 255, 0.1)` to `0 0 15px rgba(255, 255, 255, 0.15)`, keeping the inset highlight unchanged

## 2. Verify

- [x] 2.1 Open `index.html` in a browser and confirm the track outline is visibly thicker and the glow complements the border
