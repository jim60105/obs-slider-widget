## 1. CSS Fix

- [x] 1.1 Add CSS rule `select.glass-input option { background: #1a1a2e; color: #ffffff; }` to the `<style>` block in `index.html`, placed after the existing `.glass-input` rule

## 2. Verification

- [x] 2.1 Open `index.html` in a browser, click the preset dropdown, and confirm options display white text on a dark background
- [x] 2.2 Save a new preset, re-open the dropdown, and confirm dynamically added options also display correctly
