## 1. CSS Class Definition

- [x] 1.1 Add `.progress-status` class in the `<style>` block of `index.html` with `writing-mode: vertical-rl; text-orientation: mixed;`, placed alongside the existing `.progress-title` class

## 2. Apply Class to Element

- [x] 2.1 Add the `progress-status` class to the `#displayPercent` span element, keeping existing Tailwind classes (`text-sm text-white/80 self-end pb-1`)

## 3. Verification

- [x] 3.1 Verify the fraction text (e.g., "12/20") renders vertically top-to-bottom
- [x] 3.2 Verify the status text remains anchored at the bottom of the flex container
- [x] 3.3 Verify visual symmetry between the vertical title (left) and vertical status (right)
