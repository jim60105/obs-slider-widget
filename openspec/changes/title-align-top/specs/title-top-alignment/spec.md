## ADDED Requirements

### Requirement: Vertical title aligns to top
The vertical title text on the left side of the progress bar widget SHALL be aligned to the top of its flex container.

#### Scenario: Title is at the top when the progress bar is partially filled
- **WHEN** the progress bar widget is rendered with any progress value
- **THEN** the vertical title text SHALL be aligned to the top of the progress bar area
