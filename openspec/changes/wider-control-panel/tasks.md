## 1. Update Control Panel Width

- [ ] 1.1 In `index.html`, change the `<aside>` element's Tailwind class from `w-[300px]` to `w-[330px]`

## 2. Verification

- [ ] 2.1 Open `index.html` in a browser and confirm the control panel renders at 330px width
- [ ] 2.2 Confirm the display panel (`<main>`) auto-adjusts via `flex-1` with no layout breakage
- [ ] 2.3 Test the layout flip button (⇄) still works correctly with the new width
