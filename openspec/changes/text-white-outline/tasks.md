## 1. CSS White Outline Styles

- [ ] 1.1 Add multi-directional white `text-shadow` outline layers (8 directions, 1px offset, 1px blur, `rgba(255, 255, 255, 0.9)`) to the `.progress-title` rule in the `<style>` block of `index.html`, prepended before the existing theme-color glow layers
- [ ] 1.2 Add the same multi-directional white `text-shadow` outline layers to the `.progress-status` rule in the `<style>` block of `index.html`, prepended before the existing theme-color glow layers

## 2. Inline Style White Outline

- [ ] 2.1 Update the JavaScript that sets the `#displayPercent` inline `text-shadow` style to include the white outline shadow layers prepended before the theme-color glow layers

## 3. Verification

- [ ] 3.1 Verify all three text elements display a visible white outline on page load
- [ ] 3.2 Verify the white outline remains intact when the theme color is changed via the color picker
- [ ] 3.3 Verify the existing theme-color glow still displays alongside the white outline
